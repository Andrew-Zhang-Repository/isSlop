{
  "name": "isslop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local AI image detector. Build tooling lives here; the extension itself is src/.",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "@contentauth/c2pa-web": "^0.15.0",
    "esbuild": "^0.28.2"
  },
  "dependencies": {
    "exifreader": "^4.45.0"
  }
}
